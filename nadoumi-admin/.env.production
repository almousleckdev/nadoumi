VITE_APP_TITLE = Nadoumi Admin
# Same-origin path that vercel.json proxies to the backend, so the httpOnly session
# cookie is first-party. Change the rewrite destination there if the backend moves.
VITE_APP_BASE_API = /backend
