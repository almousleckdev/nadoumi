package com.nadoumi.communication.domain.enums;

/** The participant's role in the conversation, not their {@code AccessRole} on the applicant. */
public enum ParticipantRole {
    STAFF,
    APPLICANT,
    AGENT,
    GUARDIAN
}
