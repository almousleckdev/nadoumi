package com.nadoumi.application.domain.enums;

/** Opportunity shape an application pursues (DA6 — {@code SCHOLARSHIP_ONLY} deferred). */
public enum ApplicationType {
    PROGRAM_WITH_SCHOLARSHIP, PROGRAM_ONLY
}
