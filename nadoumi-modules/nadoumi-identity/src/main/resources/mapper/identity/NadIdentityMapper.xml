<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nadoumi.identity.mapper.NadIdentityMapper">

    <select id="selectUserType" resultType="string">
        select user_type from sys_user where user_id = #{userId}
    </select>

    <update id="updateUserType">
        update sys_user set user_type = #{userType}, update_time = now() where user_id = #{userId}
    </update>

    <select id="selectUserIdByUserName" resultType="long">
        select user_id from sys_user where user_name = #{userName} and del_flag = '0' limit 1
    </select>

    <select id="selectUserIdByEmail" resultType="long">
        select user_id from sys_user where email = #{email} and del_flag = '0' limit 1
    </select>

    <select id="selectUserIdByEmailAndType" resultType="long">
        select user_id from sys_user
        where email = #{email} and user_type = #{userType} and del_flag = '0'
        limit 1
    </select>

    <update id="markEmailVerified">
        update sys_user set email_verified = 1 where user_id = #{userId}
    </update>

    <update id="touchPwdUpdateDate">
        update sys_user set pwd_update_date = now() where user_id = #{userId}
    </update>
</mapper>
