<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nadoumi.identity.contact.mapper.ContactInquiryMapper">

    <insert id="insert" parameterType="com.nadoumi.identity.contact.ContactInquiry"
            useGeneratedKeys="true" keyProperty="id">
        insert into nad_contact_inquiry
            (first_name, last_name, name, email, phone, category, subject, message,
             status, source, locale, ip_address, user_agent, created_at)
        values
            (#{firstName}, #{lastName}, #{name}, #{email}, #{phone}, #{category}, #{subject}, #{message},
             #{status}, #{source}, #{locale}, #{ipAddress}, #{userAgent}, now())
    </insert>
</mapper>
