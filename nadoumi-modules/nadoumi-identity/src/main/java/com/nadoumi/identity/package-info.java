/**
 * Identity &amp; Access slice.
 *
 * <p>Phase 2 placeholder. Phase 3 adds: {@code sys_user.user_type} handling
 * (00 staff / 10 student / 20 agent / 30 guardian), the {@code /api/student/login}
 * endpoint, the {@code nad_user_applicant_access} grant model, and the
 * {@link com.nadoumi.common.access.NadoumiAccessService} implementation
 * (bean {@code @na}). See {@code docs/DOMAIN_MODEL.md} §3–§4.</p>
 */
package com.nadoumi.identity;
