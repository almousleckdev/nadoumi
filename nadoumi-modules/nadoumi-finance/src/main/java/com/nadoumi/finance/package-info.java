/**
 * Finance. Owns {@code nad_expense} (+ {@code nad_expense_category} and the
 * one-page receipt PDF) and {@code nad_revenue}. Net earnings are a derived
 * per-currency read model ({@code FinanceSummaryService}), never stored.
 */
package com.nadoumi.finance;
