package com.nadoumi.hr.domain;

/**
 * Task urgency. The admin UI colours these per the product spec:
 * LOW = orange, MEDIUM = blue, HIGH = green (most urgent).
 */
public enum TaskPriority {
    LOW,
    MEDIUM,
    HIGH
}
